source "https://rubygems.org"

gem 'rake'
gem 'rspec'
gem 'watir'
gem 'webdrivers'
gem 'faker'
gem 'ffi'
gem 'require_all'
gem 'webdriver-user-agent'

# rspec is used as BDD&TDD framework
# watir: open source ruby library for test automation
# webdrivers: bundling selenium webdrivers for different browsers
# faker: easily generate fake data for tests
# ffi: ruby ffi library
# rake: Rake is a Make-like program implemented in Ruby. Tasks and dependencies are specified in standard Ruby syntax
# webdriver user agent gem enables the usage of mobile responsive  testing
# require all enables to include a whole directory without mentioning pages explicitly